You can use `pointer arithmetics` to calculate the address of the next item in an array. Just `add the index of the item in the array to the pointer to find its location.`

```c
dose[i] === *(dose + i);
```

